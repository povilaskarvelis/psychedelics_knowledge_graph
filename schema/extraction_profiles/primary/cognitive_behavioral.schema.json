{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Primary Cognitive Behavioral Extraction",
  "type": "object",
  "additionalProperties": false,
  "required": ["schema_version", "task_id", "route_id", "study_doi", "domain_route", "paper_type", "text_depth", "extraction_status", "items", "warnings"],
  "properties": {
    "schema_version": {"type": "string", "enum": ["primary_cognitive_behavioral_v1"]},
    "task_id": {"type": "string"},
    "source_type": {"type": "string"},
    "route_id": {"type": "string"},
    "study_doi": {"type": "string"},
    "domain_route": {"type": "string", "const": "cognitive_behavioral"},
    "paper_type": {"type": "string", "enum": ["primary_study"]},
    "text_depth": {"type": "string", "enum": ["article_text", "abstract_only"]},
    "extraction_status": {"type": "string", "enum": ["extracted", "no_extractable_scoped_evidence", "wrong_source_type", "not_relevant", "human_review"]},
    "items": {"type": "array", "items": {"$ref": "#/definitions/item"}},
    "warnings": {"type": "array", "items": {"type": "string"}}
  },
  "definitions": {
    "item": {
      "type": "object",
      "additionalProperties": false,
      "required": ["compound_or_exposure", "population_or_species", "construct_or_behavior", "task_or_measure", "behavioral_context", "comparator", "dose_or_regimen", "timepoint", "statistic_or_value", "finding_summary", "evidence_location", "evidence_quote"],
      "properties": {
        "compound_or_exposure": {"type": "string"},
        "population_or_species": {"type": "string"},
        "construct_or_behavior": {"type": "string"},
        "task_or_measure": {"type": "string"},
        "behavioral_context": {"type": "string"},
        "comparator": {"type": "string"},
        "dose_or_regimen": {"type": "string"},
        "timepoint": {"type": "string"},
        "statistic_or_value": {"type": "string"},
        "finding_summary": {"type": "string"},
        "evidence_location": {"type": "string"},
        "evidence_quote": {"type": "string"}
      }
    }
  }
}
